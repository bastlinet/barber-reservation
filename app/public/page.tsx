export default function PublicHomePage() {
  return (
    <main>
      <h1>Barber Reservations</h1>
      <p>Public booking flow is still work in progress.</p>
      <p>
        Teď už adresáře nemají závorky. Simple `app/public` a `app/admin` se
        projeví ve struktuře URL jako `/public` a `/admin`, ale stále je vše
        rozdělené logicky.
      </p>
    </main>
  );
}
